<%= form_with(model: nickname, local: true) do |form| %>
  <% if nickname.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nickname.errors.count, "error") %> prohibited this nickname from being saved:</h2>

      <ul>
      <% nickname.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :nickname_name %>
  </div>

  <div class="field">
    <%= form.label :dog_id %>
    <%= form.number_field :dog_id, id: :nickname_dog_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
